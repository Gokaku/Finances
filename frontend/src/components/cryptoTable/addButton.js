import React from 'react'

function addButton() {
    return (
            <button class="addButton">
                <p class="pAddButton">ADD</p>
            </button>
    )
}

export default addButton
